status:
  xkblayout:

    label: |
      lang  = `setxkbmap -query |
               grep layout      |
               sed -e 's/layout://;'`.strip

      color = (lang == 'us' ? "greenfocus" : "notice")

      [ color, "\u270E", lang ]
